# @girk/utils

Internal utility package for the Girk monorepo.

This package is not intended for publication. It contains the small runtime
helpers shared by the generator:

- async iteration helpers
- simple string and tag parsing helpers
- filesystem helpers
- path helpers

Commands:

```bash
npm run build -w @girk/utils
npm run test -w @girk/utils
```
