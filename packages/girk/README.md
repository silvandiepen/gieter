# Girk

Girk helps you generate simple static websites, docs, and book-like sites from Markdown with almost no setup.

## Usage

```bash
npx girk
```

That command scans the current folder for Markdown files and generates a site into `public/`.

## Monorepo

The source for the package lives in [`packages/girk`](https://github.com/silvandiepen/girk/tree/master/packages/girk).

The docs site generated by this package lives in [`apps/docs`](https://github.com/silvandiepen/girk/tree/master/apps/docs).
