if menu.length > 0
    button.navigation-toggle(type="button", aria-label="Toggle navigation", aria-controls="site-navigation", aria-expanded="false")
        span.navigation-toggle__line
        span.navigation-toggle__line
        span.navigation-toggle__line
    div.navigation__background

    header.header.header--sticky
        div.header__container
            include logo.pug 

            if menu.length > 0
                include navigation.pug 
