each archive in archives   
    if archive.type == 'sections'
        include archive.section.pug     
    if archive.type == 'collection'
        include archive.collection.pug 
    else if archive.type == 'blog'
        include archive.blog.pug
    else if archive.type == 'articles'
        include archive.articles.pug