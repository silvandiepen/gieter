.toolbar
    button.color-mode(onClick="switchMode()") switch

if menu.length > 0
    input.navigation-input(type="checkbox",id="navToggle")
    label.navigation-toggle(for="navToggle")
        span
    div.navigation-background

header.header.header--sticky
    if project
    if project.logoData
        a.logo(href="/")
            | !{project.logoData}
            span=project.title
    else if project.logo
        a.logo(href="/")=project.title
            img(src=project.logo)
    else if project.title
        a.logo(href="/")=project.title

    if menu.length > 0
    nav.navigation.center
        ul.navigation__list
            each item in menu              
                li.navigation__item(class= `${item.current ? 'navigation__item--current' : ''}${item.isParent && !item.current? 'navigation__item--parent' : ''}`)
                    a.navigation__link(href= item.link)
                        span.navigation__text= item.name

